function ReadyButton() {
  // This button "prepares the player for battle" and disables the button from PlayerChoice.jsx, so the player can't switch their item after they click this button
}

export default ReadyButton;
